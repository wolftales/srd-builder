{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/wolftales/srd-builder/schemas/features.schema.json",
  "version": "3.0.0",
  "title": "Feature",
  "description": "Class feature or lineage trait from SRD 5.1",
  "type": "object",
  "required": [
    "id",
    "name",
    "simple_name",
    "owner_id",
    "page",
    "source",
    "text"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^feature:[a-z0-9_]+:[a-z0-9_]+$",
      "description": "Unique identifier: feature:{owner_simple_name}:{simple_name}"
    },
    "name": {
      "type": "string",
      "description": "Display name (e.g., 'Rage', 'Darkvision')"
    },
    "simple_name": {
      "type": "string",
      "pattern": "^[a-z0-9_]+$",
      "description": "Lowercase underscored name for indexing"
    },
    "owner_id": {
      "type": "string",
      "pattern": "^(class|lineage):[a-z0-9_]+$",
      "description": "Owning class or lineage ID"
    },
    "page": {
      "type": "integer",
      "description": "PDF page number where feature is described"
    },
    "source": {
      "type": "string",
      "description": "Source document"
    },
    "text": {
      "type": "string",
      "description": "Full feature description text"
    },
    "summary": {
      "type": "string",
      "description": "Short first-sentence summary"
    },
    "level_acquired": {
      "type": "integer",
      "minimum": 1,
      "maximum": 20,
      "description": "Level when feature is acquired (class features only)"
    }
  },
  "additionalProperties": false
}
