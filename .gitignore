__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.python-version
.venv/
venv/
.env
.cache/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
.vscode/
dist/
rulesets/*/*.pdf
rulesets/*/raw/
rulesets/*/raw/**
docs/external/

# Source-specific module-import analysis. Working notes that quote or
# reproduce third-party publication content (statblocks, keyed rooms, NPC
# names, page numbers) stay local. Public planning docs must stay synthetic.
docs/planning/local/

# macOS
.DS_Store
**/.DS_Store

# Archive folder — tracked in git as historical reference.
# (Old rules excluded most of archive/; consolidated under archive/ in v0.23.0
# and we now track the whole tree intentionally.)
archive/dist_versions/
